name: Build

on:
  push:
  pull_request:

jobs:
  macos:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Build and verify app bundle
        run: |
          ./build.sh
          './WeChat Media Exporter.app/Contents/MacOS/WeChat Media Exporter' self-test
